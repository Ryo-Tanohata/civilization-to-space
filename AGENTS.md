# AIエージェントのプロジェクト規則

## 目的と作業範囲

Civilization to Space Simulatorは探索的・象徴的シミュレーターであり、未来を一点で予言しない。R1はEarth Through Time。6時代のブラウザモックを検証してからUnityへ移行する。現在承認された段階と対象ファイルに限定して作業する。文書にある将来計画は実装の承認ではない。

## 実装前の確認

- 作業パス、既存ファイル、README、本規則、関連要件と設計を読む。
- ブランチ、origin、作業ツリーと未コミット差分を確認し、ユーザーの変更を保持する。
- 目的、変更予定ファイル、受入基準、検証方法を示す。非破壊的な未決事項は合理的な仮定を明記する。
- 新規ファイルと同名の既存ファイルがあれば勝手に上書きしない。既存ファイルの削除・改名・全面置換は承認なしに行わない。

## Git・GitHubの安全

リモートの正本はRyo-Tanohata/civilization-to-space。Privateを維持する。originが https://github.com/Ryo-Tanohata/civilization-to-space.git と一致することを確認する。GitHub認証とコミット作者は別であり、AIの身元で設定しない。作者設定は実行時にユーザーの指示を確認し、個人メールを本規則へ記録しない。

commit、push、PR、外部公開、GitHub設定・権限変更には明示承認が必要。ステージングも勝手に行わない。強制push、破壊的reset、clean、無関係な履歴の結合で問題を回避しない。所有者・ACLや広範なsafe.directory例外を変更しない。認証エラー時は秘密情報を出力せず停止する。

## 保護対象と依存関係

削除操作、外部パッケージ・CDN・ライブラリ・Unityパッケージ・Blenderアドオン導入、ProjectSettings変更、Packages変更は明示承認が必要。既存Scene、Prefab、アセットも無断で変更・削除しない。外部有料APIは使わない。APIキー、トークン、パスワード、認証情報、個人情報をファイル・リポジトリ・報告へ保存しない。

## ディレクトリの役割

- docs/：要件、計画、設計、検証・レビュー報告。
- site/：承認後のHTML/CSS/JavaScriptモック。
- unity/：承認後のUnity PersonalとC#による実装。
- blender/：承認後のBlender制作素材。

ディレクトリ名の記載は作成許可ではない。新規実装はアジャイルな縦切りで、起動・操作・レビュー可能な最小成果物にする。

## 検証と報告

変更前後の一覧と差分を確認する。文書はリンク、見出し、図、要件・仕様の整合を検証する。実装は該当する受入基準をテストし、実行していないテストを合格と書かない。失敗・未検証・仮定を区別する。

報告は「実行結果／変更ファイルと役割／検証結果／Git差分／未実行の禁止操作／重要なレビュー箇所／次の承認」の順を基本とする。ユーザーが形式を指定した場合はそれに従う。報告ファイルの追加も作業範囲を確認する。
