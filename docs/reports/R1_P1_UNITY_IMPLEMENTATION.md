# R1-P1 Unity最小再現の実装報告

状態：実装完了。機械で確かめられる検証はすべて実施した。人による体験面のレビューは未実施であり、本書では通過と扱わない。

## 対象と範囲

[R1-P1実装順序](../planning/IMPLEMENTATION_ORDER_R1_P1.md) のS1〜S4を実施した。[R1要件](../requirements/REQUIREMENTS_R1.md) のFR-06とAC-09に相当する、Unityでの最小再現である。

実装したのは、6時代のデータ読込・検証、象徴的な地球の表示、時代の選択、時系列の自動再生、視点操作、動きを減らす設定、異常時の縮退である。

## 確定した環境

| 項目 | 内容 |
|---|---|
| Unityバージョン | 2022.3.11f1（Unity Personal） |
| レンダリング | Built-in Render Pipeline。URP・HDRPを使わない |
| プロジェクト位置 | `unity/CivilizationToSpace/` |
| Scene | `Assets/Scenes/EarthTimelineDemo.unity` |
| 検証の正 | Editor Play Mode。スタンドアロンビルドは行っていない |
| 追加パッケージ | `com.unity.ugui` 1.0.0 のみ |

`com.unity.ugui` はUnityエディタに同梱されており、ダウンロードも外部通信も伴わない。依存は `com.unity.modules.ui` と `com.unity.modules.imgui` の2件で、いずれも既存である。リポジトリへ入るのは `manifest.json` と `packages-lock.json` の差分だけである。

## 構成

C#は計4,560行。Coreは描画に依存せず、Viewは時代データの値を持たない。

| 層 | ファイル | 役割 |
|---|---|---|
| Core/Json | `JsonValue`・`JsonParser`・`JsonParseException` | 外部依存を持たない最小のJSONリーダー |
| Core | `JsonRules` | `isText`／`isRatio`／`isColor`／`isCount` の受理規則 |
| Core | `CatalogNormalizer`・`CatalogDataException` | 全体エラーと縮退の切り分け |
| Core | `EraCatalog`・`EraData`・`EraVisual`・`CatalogLoadResult`・`EraCatalogLoader` | 検証済みモデルと読込 |
| Core | `EraTimeline`・`TimelinePlayback` | 時代の選択状態と自動再生 |
| View | `EarthView` | 象徴的な地球。混色と加算合成、自転、遷移の補間 |
| View | `EarthFraming`・`EarthCameraControl` | カメラの配置と視点操作 |
| View | `DemoHud`・`UiFactory`・`JapaneseFont`・`MotionSettings` | 画面と操作、日本語表示、動きの設定 |
| — | `AppRoot` | 組み立て。R2-P1が参照する唯一の入口 |
| Editor | `DemoSceneBuilder`・`EraCatalogSyncTool`・`EraCatalogProbe`・`PlayModeCheck`・`CaptureTool`・`BehaviourCheck` | 生成・同期・検証の道具 |

画面の構成はSceneのYAMLではなくコードに置いた。Sceneに保存されるのは `Main Camera`・`Directional Light`・`AppRoot` の3つだけであり、差分が読める。壊れたときは `Tools/Civilization to Space/EarthTimelineDemo シーンを生成し直す` で作り直せる。

## 自前のJSONリーダーを書いた理由

Unity標準の `JsonUtility` は、キーが存在しないことと値が `0` であることを区別できない。[データ交換形式](../design/DATA_INTERCHANGE_R1.md) は「`visual` の個別キーの欠損・型不正・範囲外は中立値へ置換して注記を出す」と定めており、`JsonUtility` ではこの規則を満たせない。

外部パッケージを増やさず規則を満たすため、`Assets/Scripts/Core/Json/` に最小のリーダーを書いた。RFC 8259 に従い、末尾カンマ・コメント・単一引用符・NaN・Infinity を受け付けない。入れ子は64段で打ち切る。重複キーは後勝ちとし、ブラウザの `JSON.parse` と同じ結果にしている。

この判断の正しさは、検証の #17（値が `null`）と #18（値が `0`）で示している。前者は縮退し、後者は縮退しない。

## データの同一性

正本は `site/data/earth-eras.json` のみである。Unityは `Assets/StreamingAssets/earth-eras.json` だけを読む。更新の向きは一方向で、Unity側から正本へ書き戻さない。

```text
git rev-parse HEAD:site/data/earth-eras.json
  → 6bfda2441b7827e3a310209822859c6025c819da
git rev-parse HEAD:unity/CivilizationToSpace/Assets/StreamingAssets/earth-eras.json
  → 6bfda2441b7827e3a310209822859c6025c819da
```

blobハッシュが一致する。`Tools/Civilization to Space/交換データのコピーを照合` でいつでも再確認できる。

照合はLF正規化後の内容で行う。`core.autocrlf` が有効な環境では、正本は作業ツリー上でCRLF、コピーは `unity/.gitattributes` の `eol=lf` によりLFになるため、生バイトの比較は環境によって食い違う。データ交換形式が「blobハッシュを正とする」と定めているのに合わせた。

## 受入基準

既存のAC系列を増やさず、`UAC-nn` を新設した。[R1-P1実装順序](../planning/IMPLEMENTATION_ORDER_R1_P1.md) は `AC-09 = UAC-04 + UAC-05 + UAC-06 + UAC-08 + UAC-12` と参照しているが、UAC表はどの文書にも定義されていなかった。本書で定義する。

| ID | 内容 | 結果 |
|---|---|---|
| UAC-01 | 追加パッケージはUnity同梱のもののみ | 通過 |
| UAC-02 | `site/` を変更しない | 通過 |
| UAC-03 | 利用者向け画面に内部パス・例外全文・スタックトレースを出さない | 通過 |
| UAC-04 | 正本とコピーのblobハッシュが一致する | 通過 |
| UAC-05 | 6時代のIDと順序が `eraOrder` と一致する | 通過 |
| UAC-06 | 名称・年代ラベル・概要・タグ・状態がJSON値と一致する | 通過 |
| UAC-07 | 全体エラー時は操作を無効化する | 通過 |
| UAC-08 | 時代を切り替えると地球と説明が同時に変わる | 通過 |
| UAC-09 | `visual` の欠損・型不正・範囲外は当該時代だけ中立値へ置換し、注記を出す | 通過 |
| UAC-10 | 日本語が画面に表示される | 通過 |
| UAC-11 | 画面の縦横比が変わっても地球と操作が画面内に収まる | 通過 |
| UAC-12 | 自動再生が仕様どおり動き、端で止まる | 通過 |
| UAC-13 | 動きを減らすと自転・周回・遷移の補間が止まる | 通過 |
| UAC-14 | 視点操作ができ、端で止まり、再生を止めない | 通過 |
| UAC-15 | 人が読んだときの受け取り方 | **未実施** |

## 検証台帳

再現できない件数を新たに作らないため、検証はすべてリポジトリ内の道具で行い、同じ手順で再実行できる。実行者はいずれもAIアシスタントである。

### U-01〜U-22 データの受理と異常系

`Tools/Civilization to Space/時代データを指定して読み込み検証` で、一時フォルダへ置いた22種の複製を読ませた。リポジトリ内のJSONは書き換えていない。

| 結果 | 件数 | 内容 |
|---|---|---|
| 全体エラー | 13 | ファイル不在、末尾カンマ、未対応schemaVersion、件数5件、ID重複、`sortOrder`重複、`sortOrder`非連番、`eraOrder`順序違い、必須文字列が空白のみ、`events`が配列でない、`presentation`欠損、`visual`が`null`、`catalogTitle`が空 |
| 縮退して継続 | 4 | `oceanLevel`欠損、`oceanLevel`=1.5、`earthColor`=`#12345`、`satelliteCount`=2.5 |
| 受理 | 5 | 正常、`oceanLevel`=`null`（縮退1件）、`oceanLevel`=0（**縮退なし**）、BOM付き、未知フィールド |

全22経路が想定どおりだった。

### U-21〜U-36 再生と視点

`Tools/Civilization to Space/再生と視点の動作を点検` で自動実行する。16件すべて通過した。

| ID | 内容 | 実測 |
|---|---|---|
| U-21 | 初期状態は停止・1倍・最初の時代 | 停止／1x／位置0 |
| U-22 | 再生を開始できる | 再生中 |
| U-23 | 1倍で約4秒に1時代進む | 4.00秒 |
| U-24 | 速度を変えても再生は続く | 再生中／2x |
| U-25 | 2倍で約2秒に1時代進む | 2.00秒 |
| U-26 | 手で時代を変えると再生が止まる | 停止／位置0 |
| U-27 | 最後の時代から再生すると最初へ戻る | 再生中／位置0 |
| U-28 | 停止を要求すると止まる | 停止 |
| U-29 | 最後の時代へ達すると自動で止まる | 停止／位置5 |
| U-30 | 動きを減らす前は自転している | 0.5秒で1.50度 |
| U-31 | 動きを減らすと自転が止まる | 0.0000度 |
| U-32 | 視点を回してもカメラが動き、再生は止まらない | 移動5.383／再生中 |
| U-33 | 寄りを変えると距離が縮む | 7.93 → 5.55 |
| U-34 | 仰角と寄りは端で止まる | 仰角70／寄り2.4 |
| U-35 | 視点をもどすと既定へ戻る | 0／0／1 |
| U-36 | 時代を切り替えてもカメラは動かない | 移動0.00000 |

時間の判定はゲーム内時間で行う。batchmodeでは実時間よりゲーム内時間が遅く進むため、実時間の固定待ちでは誤判定する。

### U-37〜U-39 画面

`Tools/Civilization to Space/6時代のキャプチャを書き出す` で、実際に描画した画面を書き出して確かめた。

| ID | 内容 | 結果 |
|---|---|---|
| U-37 | 6時代すべてで名称・年代ラベル・概要・タグ・イベント・状態が表示される | 通過 |
| U-38 | 端の時代で「前へ」「次へ」が無効になる | 通過。画素値 (36,49,67) → (18,23,32) |
| U-39 | 縦横比を変えても地球が収まる | 通過（下表） |

| 描画サイズ | 左余白 | 右余白 | 上余白 | 下余白 |
|---|---|---|---|---|
| 1280x720 | 137 | 137 | 22 | 73 |
| 1024x768 | 71 | 60 | 47 | 97 |
| 820x900 | 60 | 46 | 159 | 203 |

## 時代ごとの見分けやすさ

[R1-P0レビュー報告](R1_P0_HUMAN_REVIEW.md) は、InformationとFutureが見分けにくいことを既知の制約として受容したうえで、「Unityの発光表現はブラウザと異なるため、Unity上で改めて見え方を確認し、その結果を実装報告に記録する」としていた。本節がその記録である。

同じ手法（地球の描画領域のみを切り出した15ペアの画素差）で測った。

| 平均差 | 上位1% | ペア |
|---|---|---|
| 6.82 | 20 / 255 | **Information と Future** |
| 8.95 | 53 | Snowball と Information |
| 11.19 | 53 | Snowball と Future |
| 12.80〜32.11 | — | 中間の9ペア |
| 44.10以上 | 171以上 | Hadean を含む5ペア |

ブラウザでは平均3.56・上位1%が11だった。Unityでは6.82・20となり、**約2倍に開いたが、依然として15ペア中で最も近いペアである**。

判断は変えない。[6時代カタログ](../design/EARTH_ERA_CATALOG.md) がFutureを「同じ地球に異なる光・緑・接続」と定義しており、似ていることは設計意図である。ライティングで無理に差を付けることはしていない。区別は時代名・年代ラベル・タグ・データ品質状態のテキストが担う。

## ブラウザ仕様からの逸脱

[R1-P1実装順序](../planning/IMPLEMENTATION_ORDER_R1_P1.md) に記録済みの逸脱2・3を実装した。加えて、実装の過程で生じた判断を以下に記す。

### 記録済みの逸脱

| # | 内容 | 実装 |
|---|---|---|
| 逸脱2 | 地球をゆっくり自転させる | 毎秒3度。地球の親を回し、カメラは回さない |
| 逸脱3 | マウスでカメラを回転・ズームできる | ドラッグで回転、ホイールで寄る。仰角±70度、寄り0.55〜2.4倍で端止め |

動きを減らす代替手段として、画面に「動きを減らす」トグルを常設した。オンにすると自転・衛星の周回・時代遷移の補間が止まる。`PlayerPrefs` を使わず、セッション内のメモリにのみ保持する。

3D操作は発見可能性が低いため、操作方法を画面に常設表示している。

### 本実装で生じた判断

| # | 内容 | 理由 |
|---|---|---|
| 1 | スライダーはS3ではなくS4で追加 | 時系列の制御であり、再生と一緒に作るほうが筋が通る |
| 2 | 利用者向け文言を1箇所だけ変更 | 「この**モック**が対応していないデータ形式です」→「この**実装**が…」。判定条件は同一 |
| 3 | 右の説明パネルの幅を画面比で決める | 固定幅では狭い画面で画面外へ出る。カメラのフレーミングも同じ値を参照する |
| 4 | 地球の層を混色＋加算合成にする | 均一な半透明の殻を重ねると外側が内側を覆い、値の違いが読めなくなる |
| 5 | Playしていなくてもシーンビューに地球を出す | `DontSave` を付けており、`.unity` には保存されない |
| 6 | `presentation.storyboardCut` を読み込まない | 絵コンテのカット番号を画面へ露出させないため |

判断4は、最初の実装が失敗したことによる修正である。均一な殻を重ねた版ではInformationが白一色になり、海も植生も読めなかった。

## R1保護の規律

| 規律 | 状態 |
|---|---|
| `site/` の7ファイルを変更しない | 守った。差分0 |
| 時代のID・名称・年代ラベル・解説・視覚値をC#へ書き写さない | 守った。コードが持つ定数は対応schemaVersion、必須件数6、基準4秒、許容速度、中立値、JSONのキー名、描画側の層の色のみ |
| `eraOrder` を正本内の自己申告として扱う | 守った |
| 共通データにUnityのマテリアル名・シェーダプロパティ名を混ぜない | 守った |
| 異常系でリポジトリ内のJSONを書き換えない | 守った。一時フォルダの複製を読ませる |
| 利用者向け画面に内部パス・例外全文を出さない | 守った。全ファイルをユーザー名・絶対パスで走査して0件 |
| 数値年代を追加しない。第7の時代を追加しない | 守った |

## R2-P1への境界

R1側が公開するのは、`AppRoot.Playback` を通じた `IsPlaying`（再生中かを読む）と `RequestStop`（停止を要求する）の2つだけである。`TimelinePlayback` はこれ以外をR2-P1へ向けて公開しない。`static` も `Singleton` も作っていない。

語彙も分離した。`Scenario` はFutureの2案にのみ使い、`Condition` はR2-P1のために空けてある。

## 起動手順

1. Unity Hub で `unity/CivilizationToSpace/` を 2022.3.11f1 で開く
2. `Assets/Scenes/EarthTimelineDemo.unity` を開く
3. Play を押す

Playしなくても、シーンビューには地球が表示される。`AppRoot` の Inspector にある `Preview Era Index` を 0〜5 に変えると、表示する時代が変わる。

Game ビューは `Scale` を `1x` にして使う。`Scale` は表示倍率であり、1より大きいと画面が切れる。

## 検証の道具

| メニュー | 用途 |
|---|---|
| `EarthTimelineDemo シーンを生成し直す` | Sceneの作り直し |
| `シーンの中身を点検` | 保存対象と編集中プレビューの確認 |
| `正本から交換データを同期` | `site/data/` から `StreamingAssets/` へのコピー |
| `交換データのコピーを照合` | 正本との一致確認 |
| `時代データを指定して読み込み検証` | 任意のJSONを読ませて受理・全体エラー・縮退を確認 |
| `Play Mode で起動を確認` | 起動と読込の確認 |
| `6時代のキャプチャを書き出す` | 画面の書き出し |
| `再生と視点の動作を点検` | U-21〜U-36 の自動実行 |

batchmodeからも実行できる。描画を伴うもの（キャプチャ）は `-nographics` を付けずに起動する。

## 未実施の検証

以下は実施していない。**未実施であり、成功とは判断していない。**

- **人が読んだときの受け取り方**。UAC-15。必要な文言が画面上に存在することは確認したが、読んだ人が意図どおり理解するかは確かめていない。これは人でなければ確認できない
- **オーナー自身によるPlay Modeでの操作**。ドラッグ・ホイール・ボタンの押し心地は、実際に操作しなければ分からない
- **スタンドアロンビルド**。[R1-P1実装順序](../planning/IMPLEMENTATION_ORDER_R1_P1.md) のUG-11は本書の対象外としている
- **ブラウザとの機械的な突き合わせ**。22の異常系をapp.js側へ同じ順で流していない。判定の一致は、app.jsのソースに対して1対1で書いたことによる担保である
- **スクリーンリーダーでの読み上げ**。uGUIは支援技術に対応しない（下記の制限）
- **Windows以外の環境**。日本語フォントの解決はWindowsのフォント名に依存する

## 既知の制限

- **スクリーンリーダーに対応しない**。UnityのuGUIは支援技術へ情報を渡さない。ブラウザ側がARIAで実現していることを、Unity側では実現できない。実現不能として記録する
- **日本語フォントはOSに依存する**。`Yu Gothic UI` などWindowsの標準フォントを優先順で探す。見つからない環境では内蔵フォントへ落ち、日本語が出ない可能性がある
- **InformationとFutureは地球の見た目だけでは区別しにくい**（前掲）。設計意図であり、区別はテキストが担う
- **FR-04／AC-05（Futureの2案切替）は未実装**。将来シナリオ候補は読み取り専用の表示に留まる。ブラウザ側も未実装であり、両実装の対称性を保つために意図的に繰り延べている
- **ブラウザとUnityで見た目は一致しない**。[ブラウザモック仕様](../design/BROWSER_MOCK_SPEC.md) が「両実装の見た目の完全一致は求めない」と定めている
- **Game ビューの `Scale` は利用者の設定**。1より大きいと画面が切れる。実装側からは制御できない

## R1のDefinition of Doneに対する状態

**R1-P1は完了したが、R1全体のDefinition of Doneは未達である。**

| 項目 | 状態 |
|---|---|
| FR-06／AC-09 Unity最小再現 | **本実装で達成** |
| FR-04／AC-05 Futureの2案切替 | 未実装。ブラウザ・Unityとも |
| B06 人によるモックレビュー | [実施済み](R1_P0_HUMAN_REVIEW.md)。ただし体験面2件は未実施 |
| 人によるUnityレビュー | **未実施**。本実装に対するレビューはこれから |

R1-P1の完了は、これらを完了させたものでも取り下げたものでもない。

## 次の推奨作業

1. **人によるUnityレビュー**。オーナーが実際にPlay Modeで操作し、読み、判断する。結果は `docs/reports/R1_P1_HUMAN_REVIEW.md` に記録する
2. **FR-04／AC-05**。R1のDefinition of Doneを閉じる。ブラウザとUnityの両方へ同時に入れる
3. **R2-P1**。R2をUnityへ移す。第1層の転送契約をそのまま再利用し、ファイル名だけを変える

関連：[R1-P1実装順序](../planning/IMPLEMENTATION_ORDER_R1_P1.md)／[データ交換形式](../design/DATA_INTERCHANGE_R1.md)／[R1要件](../requirements/REQUIREMENTS_R1.md)／[R1-P0レビュー報告](R1_P0_HUMAN_REVIEW.md)／[バックログ](../planning/BACKLOG.md)
